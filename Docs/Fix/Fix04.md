### Fix and Update List 04

### Problems
1. Currently, it seems that the generated clipped DEM have the bounding box of the source DEM, generate a new bounding box based on the clipped DEM data instead.


### Update
1. For the statistics of the Clipped Basin add the stats `Valid Cells` and `No-data cells` and `Sum`.
